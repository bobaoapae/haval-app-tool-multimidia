{
  "name": "cluster-air-ui",
  "private": true,
  "version": "0.0.3",
  "scripts": {
    "dev-controls": "parcel index.html --no-cache",
    "dev": "parcel app-night.html --no-cache",
    "dev:light": "parcel app-light.html --no-cache",
    "clear": "rimraf dist",
    "build:night": "parcel build app-night.html --dist-dir dist --public-url ./ && node inline.js",
    "build:light": "parcel build app-light.html --dist-dir dist --public-url ./ && node inline.js",
    "build": "bun run clear && bun run build:night && bun run build:light",
    "prerelease": "bun run build && cp dist/app-night.html ../app/src/main/res/raw/app-night.html && cp dist/app-light.html ../app/src/main/res/raw/app-light.html"
  },
  "devDependencies": {
    "bun": "^1.3.0",
    "parcel": "^2.15.4",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "chartjs-adapter-date-fns": "^3.0.0",
    "chartjs-plugin-streaming": "^2.0.0",
    "date-fns": "^4.1.0"
  }
}
